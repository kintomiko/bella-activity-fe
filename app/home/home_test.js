'use strict';

describe('activity.home module', function() {

  beforeEach(module('activity.home'));

  describe('home controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('HomeCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});
'use strict';

describe('activity.search module', function() {

  beforeEach(module('activity.search'));

  describe('search controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('SearchCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});